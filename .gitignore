# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tooling / agent config
.claude/
.agents/

# Supabase CLI temp
supabase/.temp/

# Masters PNG de los íconos (pesados). El front usa los .webp de public/. Backup local.
icon-sources/

# Database backups / dumps (NUNCA subir — pueden tener datos de usuarios)
*.backup.gz
db_cluster-*
migration-backup/

# Knowledge graph generado por graphify (no versionar)
graphify-out/
# Backups locales del scraper
src/dataGAMES.backup-before-refresh.json
