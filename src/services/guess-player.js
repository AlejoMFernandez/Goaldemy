// Deprecated legacy service (superseded by guess-player-mcq.js and guess-player-typing.js)
// Kept as an empty shim to avoid import errors if referenced somewhere.
export function initState() { return {} }
export function loadPlayers() {}
export function nextRound() {}
export function optionClass() { return 'rounded-lg border px-4 py-2 text-slate-200' }
